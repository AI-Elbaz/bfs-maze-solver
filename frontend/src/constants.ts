import type {Point} from "./types";

export const ROWS = 15;
export const COLS = 20;
export const START_POS: Point = [1, 1];
export const END_POS: Point = [ROWS - 2, COLS - 2];
export const WALL_PROBABILITY = 0.3;
